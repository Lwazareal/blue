# TODO

- [x] Inspect existing `index.html`, `style.css`, and Vue bootstrap (`public.js`, `public-config.js`, `public-images.js`).
- [x] Create `thumbnails.html` using the same color variables + layout as `index.html`.
- [x] Create thumbnail data source in `thumbnails.js` (designer thumbnail designs).
- [x] Render thumbnail tiles with the same `image-container` / `row` / `column` styling.
- [x] Add a “Thumbnails” button on `index.html` linking to `thumbnails.html`.
- [ ] Smoke test by running the site and opening `thumbnails.html` (may require `npm install`).

